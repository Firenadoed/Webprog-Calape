{% extends 'admin/base-admin.html.twig' %}
{% block title %}Edit Collectible{% endblock %}

{% block body %}
<div class="flex items-center justify-center min-h-screen bg-[#1B1B1D]">
    <div class="form-container">
        <h2 class="text-[#d4af37] text-2xl font-bold mb-6 text-center">Edit Collectible</h2>

        {{ include('collectible/_form.html.twig', {'button_label': 'Update'}) }}

        {# Add some spacing before delete button #}
        <div class="mt-4">
            {{ include('collectible/_delete_form.html.twig') }}
        </div>

        <a href="{{ path('app_collectible_index') }}" class="back-link mt-4 block">← Back to list</a>
    </div>
</div>
{% endblock %}
