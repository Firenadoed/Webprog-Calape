{% extends 'sidebar.html.twig' %}

{% block title %}New Listing{% endblock %}

{% block body %}
<div class="page-container">
    <div class="form-container">
        <h2 class="page-title">Create New Listing</h2>

        {{ include('listing/_form.html.twig', {'button_label': 'Create'}) }}

        <a href="{{ path('app_listing_index') }}" class="back-link">Back to list</a>
    </div>
</div>
{% endblock %}